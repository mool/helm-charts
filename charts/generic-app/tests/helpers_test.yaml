suite: test helpers using deployment
templates:
  - deployment.yaml
tests:
  - it: should generate correct app name with default release name
    set:
      appName: ""
    release:
      name: my-test-release
    asserts:
      - equal:
          path: metadata.name
          value: my-test-release

  - it: should generate correct app name with custom appName
    set:
      appName: custom-app-name
    release:
      name: test-release
    asserts:
      - equal:
          path: metadata.name
          value: custom-app-name

  - it: should sanitize version strings with special characters
    set:
      image:
        tag: "v1.2.3+build.123"
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/version"]
          value: "v1.2.3-build.123"

  - it: should handle empty podAnnotations gracefully
    set:
      podAnnotations: {}
    asserts:
      - notExists:
          path: spec.template.metadata.annotations

  - it: should handle empty imagePullSecrets
    set:
      imagePullSecrets: []
    asserts:
      - notExists:
          path: spec.template.spec.imagePullSecrets

  - it: should have empty volumes when configmap disabled
    set:
      configMap:
        enabled: false
        mountPath: "/etc/config"
      volumeMounts: []
    asserts:
      - isEmpty:
          path: spec.template.spec.volumes

  - it: should not set replicas when autoscaling is enabled
    set:
      replicaCount: 5
      autoscaling:
        enabled: true
    asserts:
      - notExists:
          path: spec.replicas

  - it: should set replicas when autoscaling is disabled
    set:
      replicaCount: 3
      autoscaling:
        enabled: false
    asserts:
      - equal:
          path: spec.replicas
          value: 3

  - it: should replace plus signs in chart version for labels
    chart:
      name: generic-app
      version: "1.0.0+build"
    asserts:
      - equal:
          path: metadata.labels["helm.sh/chart"]
          value: "generic-app-1.0.0_build"

  - it: should apply default resource limits
    asserts:
      - exists:
          path: spec.template.spec.containers[0].resources
      - exists:
          path: spec.template.spec.containers[0].resources.limits
      - exists:
          path: spec.template.spec.containers[0].resources.requests

  - it: should handle all probes being disabled
    set:
      livenessProbe: {}
      readinessProbe: {}
      startupProbe: {}
    asserts:
      - notExists:
          path: spec.template.spec.containers[0].livenessProbe
      - notExists:
          path: spec.template.spec.containers[0].readinessProbe
      - notExists:
          path: spec.template.spec.containers[0].startupProbe

  - it: should handle nodeSelector configuration
    set:
      nodeSelector:
        kubernetes.io/arch: amd64
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector["kubernetes.io/arch"]
          value: amd64
